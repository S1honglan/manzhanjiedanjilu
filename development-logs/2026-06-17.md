# 2026-06-17 开发日志

## 已完成事项

1. **删除 Excel 导入功能**
   - 移除 `App.tsx` 中 `importProjectsFromExcel` 导入
   - 移除 `ChangeEvent` 类型导入
   - 移除 `importing`、`importResult`、`importInputRef` 等状态
   - 移除 `handleImportClick`、`handleImportFile` 函数
   - 移除工具栏 📥 导入按钮
   - 移除导入结果提示 UI (`importResult` 渲染块)
   - 注意：`src/excel.ts` 文件保留，后续如需导入功能可重新启用

2. **清理死代码**
   - 移除 [App.tsx:312-313] 空 JSX 占位符 `{/* ... same ... */}`

3. **修复搜索栏功能**
   - **首页全局搜索**：搜索栏和搜索结果正常可用，按角色名/昵称/备注跨所有项目搜索
   - **项目内页桌面端**：添加搜索输入框 + 状态下拉筛选，支持在项目内按角色名/昵称/备注筛选订单
   - **项目内页移动端**：🔍 按钮弹出搜索面板（含搜索历史记录），☰ 按钮弹出状态筛选浮窗

4. **CSS 更新**
   - 新增 `.search-history`、`.search-history-item`、`.btn-link` 样式
   - 新增 `.project-search-bar` 桌面端搜索栏样式
   - 移动端隐藏 `.project-search-bar`，桌面端隐藏 `.toolbar-sm`
   - 保留原有导入提示样式（`.import-notice` 等），以备后续重新启用

## 构建验证

- `tsc --noEmit`：零错误通过
- `vite build`：构建成功，35 个模块转换，513ms

## 注意事项

- `src/excel.ts` 文件未删除（仍包含完整导入逻辑），仅移除了 `App.tsx` 中的调用入口
- 搜索历史功能已完整接入（保存到 localStorage，支持清空）
