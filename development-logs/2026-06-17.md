# 2026-06-17 开发日志

## 已完成事项

1. **删除 Excel 导入功能**
   - 移除 `App.tsx` 中 `importProjectsFromExcel` 导入
   - 移除 `ChangeEvent` 类型导入
   - 移除 `importing`、`importResult`、`importInputRef` 等状态
   - 移除 `handleImportClick`、`handleImportFile` 函数
   - 移除工具栏 📥 导入按钮
   - 移除导入结果提示 UI (`importResult` 渲染块)
   - 注意：`src/excel.ts` 文件保留，后续如需导入功能可重新启用

2. **清理死代码**
   - 移除空 JSX 占位符 `{/* ... same ... */}`
   - 移除 `expandStatusId` 状态及卡片展开逻辑（改为下拉选择器后不再需要）

3. **修复搜索栏功能**
   - 首页全局搜索：桌面端搜索栏 + 移动端 🔍 弹出搜索面板
   - 项目内页桌面端：搜索输入框 + 状态下拉筛选
   - 项目内页移动端：🔍 搜索面板 + ☰ 状态浮窗
   - 移除搜索历史记录功能（简化交互）

4. **重构编辑订单弹窗 UI（参考图风格）**
   - 拍摄角色：大号加粗输入框
   - 客户昵称 | 拍摄张数：双列布局
   - 定金 | 全部收入：双列布局
   - 编辑模式下备注以"附加项"分区卡片展示
   - 订单状态：分段按钮组替换 select 下拉框
   - 底部左右分布：删除在左，取消/保存在右

5. **漫展日期改为日期段**
   - `ExpoProject` 类型新增 `dateEnd` 字段
   - 编辑项目面板：开始日期/结束日期分开的年月日下拉选择器
   - 卡片展示：`📅 开始 ~ 结束` 格式
   - 修复 `data.ts`、`excel.ts` 中缺少 `dateEnd` 的类型错误

6. **订单卡片状态下拉选择器 + 莫兰迪色系**
   - 状态下拉 `<select>` 替换旧的标签点击展开模式
   - 莫兰迪色系背景：预付定金=蓝色 `#d6dfe8`，工作中=黄色 `#ede4ce`，交付=绿色 `#d2dfd0`
   - 下拉按钮颜色：蓝 `#9dafc2`，黄 `#c4a960`，绿 `#8faa8b`
   - 卡片背景使用 `!important` 固定色值，不受暗色模式影响

## 构建验证

- `tsc --noEmit`：零错误通过
- `vite build`：构建成功，35 个模块转换，593ms

## 提交记录

- `9b429c6` refactor: 删除Excel导入+修复搜索栏+重构编辑订单弹窗UI
- `54afa13` feat: 漫展日期改为日期段+年份选择器
- 待提交：订单卡片状态下拉选择器+莫兰迪色系
